name: Deploy with CA Cert

on: push

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up CA Cert
        env:
          CA_CERT: ${{ secrets.CA_CERT }}
        
      - name: Use CA Cert in Deployment Script
        run: |
          echo "Using CA Cert at $CA_CERT"
          # Ваши команды для деплоя здесь
